package carnerero.agustin.cuentaappandroid.presentation.ui.accounts.add.layouts

/*
@Composable
fun LandScapeLayout(
    selectedCurrency: String,
    isDropdownExpanded: Boolean,
    onDropdownExpandedChange: (Boolean) -> Unit,
    onCurrencySelected: (String) -> Unit,
    onConfirm: () -> Unit,
    onBack: () -> Unit
) {
    Row(
        modifier = Modifier
            .fillMaxSize()
            .padding(dimens.extraLarge)
    ) {
        // IZQUIERDA
        Column(
            modifier = Modifier
                .weight(1f)
                .fillMaxHeight(),
            horizontalAlignment = Alignment.CenterHorizontally,
            verticalArrangement = Arrangement.Center
        ) {
            CreateAccountForm(

            )
        }

        Spacer(modifier = Modifier.width(32.dp))

        // DERECHA
        Column(
            modifier = Modifier
                .weight(1f)
                .fillMaxHeight(),
            horizontalAlignment = Alignment.CenterHorizontally,
            verticalArrangement = Arrangement.Center
        ) {
            CurrencyAndActions(
                createAccountViewModel,
                navToLogin,
                navToBack
            )
        }
    }
}
*/